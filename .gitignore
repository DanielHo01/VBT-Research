# Models — keep .onnx and .pt but NOT backups
models/*.onnx
models/*.pt
!models/*.onnx
!models/*.pt

# Datasets — too large for git
datasets/weightlifting-plates/
datasets/owlvit_pseudo/
datasets/interactive_labels/

# Archives — old experiment debris
_archive/
archives/

# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Virtual environments
.venv/
venv/

# IDE
.vscode/
.idea/

# Windows
*.tmp
*.log

# Validation raw videos — distributed via GitHub Releases (not git)
# Download: https://github.com/DanielHo01/VBT-Research/releases/tag/v1.0-videos
#   gh release download v1.0-videos --pattern "raw_videos.zip" --dir .
#   unzip raw_videos.zip  -> produces validation/dataset_benchmark/raw_videos/
validation/dataset_benchmark/raw_videos/

# YOLO cache
runs/detect/plate_v1_run/

# Pre-trained weights (keep model files but not backups)
yolov8n.pt
*.onnx.backup
*.pt.backup
models/*.onnx.backup
models/*.pt.backup


# Analysis outputs
detection_distributions.json

# One-off training scripts
train_quick_test.py
